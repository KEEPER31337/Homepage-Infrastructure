server {
    listen       80;
    server_name  localhost;

    server_tokens off;

	autoindex off;

    location / {
        root /home/keeper;
		index index.html index.htm;
		try_files $uri /index.html;
    }
}